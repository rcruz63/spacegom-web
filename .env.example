# AWS (DynamoDB). Cargado vía load_dotenv en app/main.py.
# Copia a .env y rellena con tus valores. No subir .env a git.

AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_access_key
AWS_REGION=eu-west-1

# Opcional: DynamoDB local (ej. Docker)
# DYNAMODB_ENDPOINT_URL=http://localhost:8000
